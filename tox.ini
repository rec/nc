[flake8]
ignore =
    # whitespace before ':'.  See https://github.com/python/black/issues/315
    E203,

    # multiple imports on one line
    E401,

    # do not use bare except
    E722

exclude = build/*, *.ini, *.in, MANIFEST*, *.md, .eggs, doc/*
max-complexity = -1
